# 메이투투 스마트팩토리 — 프로젝트 메모리

이 파일의 내용은 모든 Claude Code 세션 시작 시 자동 로드됩니다.

## 핵심 도메인 규칙

### 자재 / 레시피
- **SOP-002 (앙호두 / 호두과자 전용반죽)** 배합비 (`SOP레시피` 시트):
  - RM001 계란 — 0.1765 (정밀) / 0.18 (반올림 — 시트 정정 필요)
  - RM002 물    — 0.1765 / 0.18
  - **RM011 앙브레드호두과자 전용믹스** — 0.5882 / 0.59
    ⚠️ 2026-08 자재 교체: `RM003 마미만쥬믹스`(비활성) → **`RM011`**. 코드에 믹스 코드를 박지 말 것 —
    주 자재는 **배합비 최대**로 찾는다(`bom-calc`·`권장 배치`). 두 믹스는 **공급사가 다른 별개 자재**다.
  - RM006 식용유 — 0.0588 / 0.06
    ⚠️ 품목제조보고서(202602733083) 등록명은 **「콩기름(대두유)」** — 시트 표시명 `식용유`와 다르다.
  - 합계 정밀 = 1.0000, 반올림 = 1.01 (안전계수 중복 적용 위험)
- **공식**: `목표 투입량 = 배합비 × 완제품kg × 1.01` (코드 line 4570)

### RM002 물 — 재고 추적 외
- **수도 공급 자재** — 시트에 LOT 안 만듦, 잔량 0 이 정상
- BOM 계산기 / WO 생성 BOM 검증에서 **부족 표시 금지**
- 표시: "관리대상 외" 또는 "수도 공급 · 추적 안함"

### 프리믹스 (RM011 앙브레드호두과자 전용믹스) 투입 제약
- **10kg 단위(=정수 포대)로만 투입 가능** — 물리적 제약
- 권장 배치 추천 시 정수 포대로 떨어지는 봉수만 후보

### 표준 배치 사이즈
- 봉당 5kg (포장 단위 고정)
- **추천 범위**: 200~350kg = 40~70봉
- 정밀 배합비 적용 후 47봉(235kg, 14포대) 이 최우수 (잔여 0.41kg)

### 원재료 발주점(안전재고) — 손으로 박지 말 것
- `원재료품목[9] 발주점` 은 「현재고 ≤ 발주점 → 발주필요」 알람의 **유일한 기준**이다.
- 값은 **실사용·실입고에서 뽑는다**: `발주점 = 일평균사용 × (리드타임 + 실측발주주기/2) + 1.65σ√(P/7)`
  → SKU 포장단위로 올림. 산식 = `index.html` `// <reorder-calc>` · 검증 `node tests/reorder-point.test.js`.
- **산식은 «제안»까지만 한다.** 정본은 `#items-rm` → ✏️ 발주점에서 사람이 확정한 시트 값 — 숫자는 냉장고 용량도
  유통기한도 모른다. 모달이 근거(평균 사용량·리드타임·실측 주기·안전여유)를 같이 띄우니 그걸 보고 정한다.
- ⚠️ 시트 날짜를 파싱할 때 ` `→`T` 로 ISO 흉내를 내지 말 것 — 한 자리 시(`8:59`)가 통째로 버려진다(입고 68건 중 9건 실종).
- 사용 이력이 없는 자재(앙버터용 RM008·009·010)는 **비워 둔다**. 화면 뱃지는 「정상」이 아니라 「발주점 미설정」.
- 2026-08-21 재산정 실측치·남은 과제 = `docs/FACTORY_MASTER_DATA.md` §9.

### 주간 생산량 제안 — 감이 아니라 실판매에서
- 홈 「🏭 이번 주 권장 생산」 카드(`loadHomeProdPlan`) · 산식 = `index.html` `// <prod-plan>`.
  `권장 = max(0, D×(안전재고 2주 + 다음주 1주) − 가용재고)` → 배치 40봉(200kg) 단위 올림.
  `D` = 마켓봄 실판매 최근 8주 **선형가중** 이동평균(최근 주가 무겁다). 가용재고 = 출하가능 잔량 + 냉동보관중·진행중 WO.
- ⚠️ **재고가 목표보다 많으면 0배치가 정답이다.** 늘 뭔가 만들라고 말하는 제안은 과잉재고를 굳힌다.
- ⚠️ 수요는 **마켓봄 실판매**(가맹점 주문)를 쓴다. 공장 출하는 물류로 «밀어낸» 양이라 실수요보다 크고
  (2026-08 실측 주 17봉·12주 누적 207봉), 그 숫자로 계획하면 과잉생산이 스스로를 정당화한다.
- ⚠️ 마켓봄↔완제품 연결은 **`완제품품목[19] 마켓봄코드`**(쉼표구분)만 본다. **품명으로 추측하지 말 것** —
  마켓봄 「앙호두 전용반죽」 vs 마스터 「앙호두(호두과자 전용반죽)」은 `normName` 으로도 안 맞고,
  앞글자 일치로 풀면 앞치마·모자·쿠폰 같은 굿즈가 반죽 수요로 딸려 들어온다. FG002 = `ANG00002,ANG00266,ANG00276`.
  연결이 비면 공장 출하 실적으로 떨어지고 화면에 「출하 기준」이라고 밝힌다.
- 같은 산식이 ERP 공장 도메인(`#/domain/06`)에도 있다 — **한쪽만 고치면 두 화면이 다른 숫자를 말한다.**
  검증: `node tests/prod-plan.test.js` (이 repo) + `node tests/factory_prod_plan.test.mjs` (erp repo) — 기준선 숫자가 같다.

### HACCP 운영 규칙
- 냉동 보관 최소 12시간 후 출하 승인 가능
- **소비기한은 품목별로 다르다** — 앙호두(호두과자 전용반죽) 6개월 / 앙버터 호두과자 12개월.
  `완제품품목[5] shelfLife` 를 쓰고 **6개월을 상수로 가정하지 말 것**
- 공정기록·LOT 추적 무결성 필수 (자동 정합성 검증 + 수동 audit)

### ⚠️ CCP 번호 — 기준서 26.08.05 에서 재배정됨
정본 = 「메이투투 해썹관리기준서 26.08.05」(제정 2026.03.30 → 개정1 2026.06.02 가열 CCP 추가).

| | 금속검출 | 동결 | 가열 |
|---|---|---|---|
| **현행 (기준서 26.08.05)** | **CCP-1P** | **CCP-2B** | **CCP-3B** |
| 구 번호 (1차 인증) | CCP-2P | CCP-1B | — |

**1과 2가 서로 바뀌었다.** 화면·PDF·토스트에 CCP 번호를 하드코딩하지 말고 반드시 `CCP.METAL.no`
/ `CCP.FREEZE.no` / `CCP.HEAT.no` (또는 `ccpLabel()`) 를 쓴다 — 앱이 뽑는 기록부 PDF 가 그대로
HACCP 감사 바인더로 들어가므로 번호가 기준서와 다르면 심사 지적 대상이다.

- 시트명·필드키(`CCP_동결온도`, `CCP_금속검출`)와 화면 해시(`#ccp-dev`)는 **기존 기록 호환을 위해 옛 이름 그대로 둔다.** 표시 문자열만 상수를 통해 바꾼다.
- 한계기준: 금속 `Fe 2.5mm∅ · STS 4.0mm∅ 이상 불검출` / 동결 `−18℃ 이하 · 12시간 이상` /
  가열 `175~190℃ · 2분30초~3분 · 가열 후 품온 90℃ 이상`(앙버터 호두과자 전용)
- 런타임 기준값의 SSOT 는 시트 **`HACCP기준`** 이다. 코드의 `CCP` 상수는 시트를 못 읽을 때의 fallback이자
  "시트와 코드가 어긋났는지" 판정하는 기준선 — 기준이 바뀌면 **시트와 상수를 함께** 고친다.
- 기준·성적서·편차 화면 = `#haccp-std`. 판정 로직 검증: `node tests/haccp-std.test.js`
- **Drive 폴더 ID 는 `DRIVE` 상수 하나에만 둔다** (`index.html` CONFIG 위). 공장 문서의 SSOT = 공유드라이브 `07_공장`.
  `DriveDocs.scan()` 이 제품별 성적서 4슬롯 + 기기 13대 검교정 폴더를 훑어 **원본 보유 여부**를 본다.
  ⚠️ 기기 폴더는 `기기/회차 폴더/파일` 로 **한 겹 더 깊다** — 얕게 보면 전부 '없음'이 된다.
  ⚠️ **파일명·폴더명에서 유효기한을 추측하지 말 것.** 만료 판정 SSOT 는 시트(`시험성적서`·`기기관리`)다.
  폴더 트리·ID 표 = `docs/FACTORY_MASTER_DATA.md` §1-1~1-3. 검증: `node tests/drive-docs.test.js`
- **성적서·검교정 원본은 앱에서 올린다** (`#haccp-std` 성적서 탭 · 실무자). 업로드 뒤 유효기한/차기 검교정일을
  시트에 넣는 데까지가 한 흐름이다 — **파일만 올리면 만료 알람이 안 울린다.** 삭제 기능은 두지 않는다(기록 보관 2년).
  ⚠️ 붙여넣기·드롭 필터(`_sfAcceptsFile`)는 input 의 `accept` 를 따른다. 이미지로 하드코딩하면 PDF 성적서가 조용히 버려진다.
- 🔕 **시험성적서 만료·임박 알림은 기기별로 끌 수 있다** (`CertNotif` · `// <cert-notif>` 블록 · 건의 FR-20260813-3ZC).
  끄는 것은 **표시(홈 토스트 + 홈 🧪 목록 + 상태 카드)뿐**이다 — 만료 판정(`certStatus`)·성적서 탭·홈 상태 카드(🧪 N건)는
  그대로 둔다. 만료 성적서 = 원·부재료 입고검사 기준 미충족이라 **사실 자체를 숨기면 HACCP 근거가 흔들린다.**
  ↳ 그래서 상태 카드를 숨겨도 **건수는 안 사라진다** — 홈의 「🔕 시험성적서 알림 꺼짐 (N건)」 줄이
  그 자리를 대신한다(FR-20260813-QMX). 「알림」이라 적힌 칸이 꺼도 남아 있는 게 신고의 요지였다.
  기본값은 **켜짐**이고, 알 수 없는 값·localStorage 차단도 전부 켜짐으로 떨어진다(애매하면 알리는 쪽).
  다시 켜는 자리를 **두 곳**(홈 «🔕 알림 꺼짐» 배너 · 설정 › 알림) 둔 이유 — 알림이 0건인 날엔 홈 배너가 없어
  설정밖에 길이 없다. 검증: `node tests/cert-notif.test.js` (판정·배선·재활성 경로)
- **`stepType='heat'`** = 가열공정 CCP-3B 기록(호기별 행). 판정은 `heatJudge()` 하나만 쓴다 —
  온도·시간 숫자를 화면·검증에 다시 쓰지 말 것. 이탈 시 개선조치 미기재면 공정 완료가 막힌다.
- 앙버터 호두과자 SOP 는 `#haccp-std` 「SOP 생성」(`hsSeedAngbutterSop`)으로 기준서 값에서 한 번에 만든다 — 손으로 옮겨 적지 말 것.
- 공장 기준정보 원천(품목보고번호·배합비·설비·성적서) = `docs/FACTORY_MASTER_DATA.md`

### UI 함정
- **`.btn-outline` 은 단독으로 쓰면 안 된다** — `.btn` 기본값인 흰 글씨·흰 테두리라 흰 카드 위에서 보이지 않는다.
  반드시 색상 modifier 와 짝짓는다: `btn-outline btn-pri` / `btn-gray` / `btn-info` / `btn-warn` / `btn-err`.
  (2026-08-05 라이브 검수에서 버튼 4개가 통째로 안 보이던 실장애)

## 데이터 모델

### 시트 컬럼 인덱스
- `작업지시서`: [0]=woId, [1]=sopId, [3]=fgItemCode, [4]=name, [5]=targetQty, [7]=packCount, [8]=email, [9]=시작, [10]=완료, [11]=냉동시작, [12]=상태, [13]=lastStep, [14]=fgLotId, [15]=승인자, [16]=승인시각, [17]=비고, [19]=pdfUrl
- `원재료LOT`: [0]=ID, [1]=itemCode, [4]=qty, [5]=unit, [6]=mfgDate, [7]=expDate, [8]=remain, [9]=status, [10]=indivCount, [11]=temp
- `완제품LOT`: [0]=ID, [1]=itemCode, [3]=qty, [5]=prodDate, [6]=expDate, [7]=woId, [8]=remain, [9]=status, [10]=packCount
- `원재료개별 / 완제품개별`: [0]=indivId, [1]=lotId, [2]=itemCode, [3]=status, [4]=qty/dt, [5]=note
- `완제품품목`: [8]=packsPerBox, [10]~[17]=규제정보(`FG_EXT`), **[18]=공급단가(원/봉·VAT 제외)** — 손익 매출의 유일한 단가 소스
- `원재료품목`: [13]=매입단가(원/기본단위) — ERP `SmartFactoryKpi.rawInventory()` 도 이 칸을 읽는다
- `공장손익`(자동 생성): [0]=period(YYYY-MM), [1]=항목(노무비/임차료·관리비/공과금(전기·가스·수도)),
  [2]=금액, [3]=메모, [4]=등록자, [5]=등록일시. **금액이 빈 칸 = 수기값 없음**(0 이 아니다 — ERP 결산으로 폴백)

### 공장 손익 (`#pnl` · 관리자 전용)
**제조이익 = 매출 − (재료비 + 노무비 + 제조경비)**. 집계는 `FactoryPnl.build()` 하나뿐 — 화면은 그리기만 한다.
값마다 출처 배지(`_pnlSrc`)를 찍는다. **추정을 실적처럼 보이게 하지 않는 게 이 화면의 원칙.**

**원가 7줄** = `FactoryPnl.LINES`(키·라벨·색). 여기 한 줄 추가하면 표·막대·합계가 같이 따라온다.

| 항목 | 1순위 | 2순위 |
|---|---|---|
| 매출 | 마켓봄 반죽 실매출 — ERP `/api/logistics?only=marketbom` 의 `supply`(공급가) | 출하기록 × `완제품품목[18]` 공급단가 (추정) |
| 원재료비 / 부재료·포장재비 | `공정기록`(stepType='input') `stepData.inputQty` × `원재료품목[13]` — 둘의 구분은 **`원재료품목[5] category`**(`PNL_SUBMAT_RE`) | 투입기록 없는 달은 `구매주문` 매입 기준 |
| 소모품비 | `구매주문` 중 `소모품품목` 코드 — 소모품은 투입기록이 없어 **산 달**에 잡힌다 | — |
| (원재료·부자재 폴백) | 실투입·매입 둘 다 없으면 **표준원가** — 원재료 `PNL_RAW_PER_PACK`(봉당 12,388) · 부자재 `PNL_SUBMAT_PER_BOX`(박스당 2,480) + `PNL_SUBMAT_PER_PACK`(봉당 461.5) | 시트 15kg 박스값을 박스고정/봉당으로 쪼갠 것 |

- ⚠️ **박스 규격이 SKU 마다 다르다** — `ANG00266` = **4봉 20kg**, `ANG00276` = 3봉 15kg(2026-07 전환·사용자 확정 2026-08-12).
  박스수에 15kg 원가를 그대로 곱하면 4봉 박스가 팔린 달의 원가가 **25% 과소 계상**된다.
  → 원재료·부자재는 **봉수**(`mbPacks`)로, **택배비만 규격 무관 박스당 정액**으로 계산한다.
  봉수 판정 = `packsPerBoxOf()` — 품명 규격표기(`(5kg*3ea)`) 우선, 없으면 박스공급가 ÷ 봉당단가 역산(옛 SKU 는 품명에 규격이 없다).
| 폐기손실 | `원재료개별[3]='폐기'` × 매입단가. 수량 = `원재료LOT[4]÷[10]`(폐기되면 개별 `[4]`가 날짜로 덮여 수량을 잃는다) | — |
| 택배비 | `공장손익` 시트 수기 | ERP `Closing_Line` 운반비(`PNL_FREIGHT_RE`) → **출고 박스수 × `PNL_FREIGHT_PER_BOX`(3,800원)** |
| 노무비·제조경비 | `공장손익` 시트 수기(사람이 고친 값이 이긴다) | ERP 재무DB `Closing_Line` 매출원가 › 인건비(공장)·공장경비 |

- ⚠️ **완제품 폐기는 원가에 넣지 않는다** — 그 반죽을 만든 원재료가 이미 실투입으로 계상돼 있어 이중계상이 된다.
  매출로 이어지지 못한 **물량 지표**(`fgScrapPacks`)로만 보여준다. 원재료 폐기는 공정 input 에 안 잡히므로 반대로 **더해야** 한다.
- ⚠️ 마켓봄 `deliveryByMonth`(월 7~13만)는 **굿즈 소량 배송분**이라 반죽 냉동택배가 아니다 — 폴백으로 쓰면 원가가 1/8 로 깎인다.
  박스당 **3,800원**(3봉×5kg 건당·공장 프로젝트 시트와 동일)에 출고 박스수를 곱한다.
  ⚠️ 같은 시트의 「배송비(물류) 9,108원」은 콜로 채널용 — **반죽은 더 이상 콜로로 안 나간다**(2026-08-12). 끌어오지 말 것.
- 📦 **재고 카드** = `FactoryPnl.inventory()` — `원재료LOT[8]` 잔량 × `원재료품목[13]` 매입단가, 분류로 원재료/부재료를 가른다.
  ⚠️ **날짜별 재고원장이 없어 시점 스냅샷만** 낸다(월별 기말재고 아님). 제외 상태(폐기·소진·입고취소)는
  ERP `rawInventory()` 와 같게 맞춘다 — 갈리면 두 시스템 재고금액이 어긋난다. 발주점 미달·단가 미입력은 경고로 올린다.
- ⚠️ 품목 마스터(원재료·소모품)에 없는 매입은 `unknownBuy` 로 빠져 어느 원가에도 안 들어간다 — 화면이 경고한다.

- 매출 대상 품목 = **`완제품품목` 활성 품목의 품명과 맞는 마켓봄 판매행**(`isOurProduct`). 코드가 아니라 **품명**으로
  맞춘다 — ⚠️ 반죽은 SKU 교체 시 품명에만 규격이 붙어(`앙호두 전용반죽(5kg*3ea)`) 코드로 맞추면 끊긴다.
  완제품을 새로 등록하면(앙버터 등) 코드 수정 없이 자동으로 잡힌다.
- `구매주문[9] totalAmt` 합(=실매입·현금 기준)은 **참고로만** 보여주고 손익에 넣지 않는다(차이 = 재고 증감).
- ⚠️ **물량은 전부 봉(1봉 5kg)으로 정규화**한다(`FactoryPnl.toPacks`). 시트마다 kg·박스가 섞여 있어
  그대로 더하면 배수로 틀린다(ERP 재고평가에서 실제로 3배 과대평가 사고). 단위가 `kg`면 ÷5, `box`면 ×packsPerBox.
- ⚠️ **매출·원가 모두 공급가(VAT 제외)**. 매출만 부가세를 포함하면 이익이 10% 부풀려진다.
  마켓봄 판매가 23,750원/봉(VAT 포함) = 공급가 `PNL_DEFAULT_PRICE` 21,591원/봉 — 같은 가격, 다른 기준일 뿐이다.
- **노무비 = 급여대장 지급합계 + 사업주 부담 4대보험**(`PNL_EMPLOYER_INS_RATE` = **10.2%**, 입력 모달의 `＋보험료` 버튼).
  근거 = 2026-05~07 메이투투 급여대장 공제액 역산 — 국민연금·건강·장기요양·고용(실업급여)은 **노사 50:50** 이라
  근로자 공제액과 같은 금액을 회사도 내고, 여기에 **고용안정·직능 0.25%**(150인 미만) + **산재 약 1.0%**(식료품 제조업)가 붙는다.
  실측 10.25 / 10.22 / 10.01% → 10.2%. ⚠️ 회사 부담분이 다른 계정에 이미 잡혀 있으면 이중계상이니 버튼을 안 누른다.
  ⚠️ 버튼은 **한 번만** 먹는다(두 번이면 21%). `disabled` 만 믿지 말고 핸들러 안에서도 막을 것 — 테스트가 이 회귀를 잡는다.
- 🔐 **급여대장(ERP `Payroll`)은 이 앱이 읽을 수 없다** — `erp-secrets` 키가 개인 브라우저 localStorage 에만
  있어 서버도 이 앱도 복호화 못 한다. 키를 여기 심으면 급여 전체가 샌다. **결산 계정을 읽는 게 유일한 자동 경로.**
- 감가상각은 넣지 않는다(사용자 확정 2026-08-12). 판관비·이자도 제외 — 여기 이익은 제조이익이지 순이익이 아니다.
- 검증: `node tests/factory-pnl.test.js` (36건)
- 📄 박스당 표준원가의 원천 = Drive `07_공장/공장 프로젝트_물반죽` 시트 **`손익 계산 (26.06.29)`** 탭
  (공급가 72,000 / 원재료 37,164 / 부자재 3,864 / 배송 3,800 → 박스당 손익 27,172원). 단가가 바뀌면 여기부터 본다.. ERP 쪽 CORS 회귀 = maytutu-erp `tests/logistics_cors.test.mjs`

### 상태값 (status 필드)
- WO `[12]`: 진행중 / 승인대기 / 냉동보관중 / 출하가능 / 출하완료 / 승인완료 / 거부-재작업 / 거부-폐기 / **삭제**
- 원재료 LOT `[9]`: 미사용 / 사용중 / 소진 / 폐기 / **입고취소**
- 완제품 LOT `[9]`: 냉동보관중 / 출하가능 / 출하완료 / 폐기 / 소진 / **삭제** / **채번예약** (atomic claim placeholder)

### LOT ID 규칙 (`generateLotId`)
- 형식: `{prefix}-{itemNum}-{mfgYY MM DD}-{expYY MM DD}-{NNN}`
- 예: `FG-002-260427-261027-001` (FG002, 2026-04-27 제조, 2026-10-27 만료, 1번)
- **Atomic Claim**: QR라벨 단계에서 채번 즉시 placeholder row 적재 → 동시 채번 충돌 방지

## 환경

- **Repo**: `doganzi/maytutu-smartfactory` (private)
- **배포**: GitHub Pages 자동 (main 브랜치) → https://doganzi.github.io/maytutu-smartfactory/
- **백엔드**: Google Sheets + Drive (사용자 OAuth, 클라이언트 사이드)
- **단일 파일**: `index.html` (~430KB) + `manifest.json` (PWA)
- **소스 외 접근**: Sheets ID `1_koe58lReouU_ZZxzUh5SU8gUkuHjlwb1LNjZbJGSLU`
  - Claude Code 환경에서 직접 접근 불가 (OAuth 자격증명 없음)
  - 데이터 확인 필요 시 사용자가 시트 일부를 채팅에 붙여넣어 줘야 함

## 개발 / PR 규칙

- 개발 브랜치: `claude/continue-smart-factory-eiuWj`
- 머지: PR → squash merge (선형 히스토리)
- 모든 변경 전 진행중 작업 영향 점검 — wo-execute / 진행중 WO 데이터 보존 우선
- LiveSync 자동 재렌더는 입력 화면 (`wo-create`, `bom-calc`, `req-form`, `data-audit`)에서 스킵 — 입력값 보존
