# 메이투투 스마트팩토리 — 프로젝트 메모리

이 파일의 내용은 모든 Claude Code 세션 시작 시 자동 로드됩니다.

## 핵심 도메인 규칙

### 자재 / 레시피
- **SOP-002 (앙호두 / 호두과자 전용반죽)** 배합비 (`SOP레시피` 시트):
  - RM001 계란 — 0.1765 (정밀) / 0.18 (반올림 — 시트 정정 필요)
  - RM002 물    — 0.1765 / 0.18
  - RM003 마미만쥬믹스 — 0.5882 / 0.59
  - RM006 식용유 — 0.0588 / 0.06
  - 합계 정밀 = 1.0000, 반올림 = 1.01 (안전계수 중복 적용 위험)
- **공식**: `목표 투입량 = 배합비 × 완제품kg × 1.01` (코드 line 4570)

### RM002 물 — 재고 추적 외
- **수도 공급 자재** — 시트에 LOT 안 만듦, 잔량 0 이 정상
- BOM 계산기 / WO 생성 BOM 검증에서 **부족 표시 금지**
- 표시: "관리대상 외" 또는 "수도 공급 · 추적 안함"

### 마미만쥬믹스 (RM003) 투입 제약
- **10kg 단위(=정수 포대)로만 투입 가능** — 물리적 제약
- 권장 배치 추천 시 정수 포대로 떨어지는 봉수만 후보

### 표준 배치 사이즈
- 봉당 5kg (포장 단위 고정)
- **추천 범위**: 200~350kg = 40~70봉
- 정밀 배합비 적용 후 47봉(235kg, 14포대) 이 최우수 (잔여 0.41kg)

### HACCP 운영 규칙
- 냉동 보관 최소 12시간 후 출하 승인 가능
- **소비기한은 품목별로 다르다** — 앙호두(호두과자 전용반죽) 6개월 / 앙버터 호두과자 12개월.
  `완제품품목[5] shelfLife` 를 쓰고 **6개월을 상수로 가정하지 말 것**
- 공정기록·LOT 추적 무결성 필수 (자동 정합성 검증 + 수동 audit)

### ⚠️ CCP 번호 — 기준서 26.08.05 에서 재배정됨
정본 = 「메이투투 해썹관리기준서 26.08.05」(제정 2026.03.30 → 개정1 2026.06.02 가열 CCP 추가).

| | 금속검출 | 동결 | 가열 |
|---|---|---|---|
| **현행 (기준서 26.08.05)** | **CCP-1P** | **CCP-2B** | **CCP-3B** |
| 구 번호 (1차 인증) | CCP-2P | CCP-1B | — |

**1과 2가 서로 바뀌었다.** 화면·PDF·토스트에 CCP 번호를 하드코딩하지 말고 반드시 `CCP.METAL.no`
/ `CCP.FREEZE.no` / `CCP.HEAT.no` (또는 `ccpLabel()`) 를 쓴다 — 앱이 뽑는 기록부 PDF 가 그대로
HACCP 감사 바인더로 들어가므로 번호가 기준서와 다르면 심사 지적 대상이다.

- 시트명·필드키(`CCP_동결온도`, `CCP_금속검출`)와 화면 해시(`#ccp-dev`)는 **기존 기록 호환을 위해 옛 이름 그대로 둔다.** 표시 문자열만 상수를 통해 바꾼다.
- 한계기준: 금속 `Fe 2.5mm∅ · STS 4.0mm∅ 이상 불검출` / 동결 `−18℃ 이하 · 12시간 이상` /
  가열 `175~190℃ · 2분30초~3분 · 가열 후 품온 90℃ 이상`(앙버터 호두과자 전용)
- 런타임 기준값의 SSOT 는 시트 **`HACCP기준`** 이다. 코드의 `CCP` 상수는 시트를 못 읽을 때의 fallback이자
  "시트와 코드가 어긋났는지" 판정하는 기준선 — 기준이 바뀌면 **시트와 상수를 함께** 고친다.
- 기준·성적서·편차 화면 = `#haccp-std`. 판정 로직 검증: `node tests/haccp-std.test.js`
- **Drive 폴더 ID 는 `DRIVE` 상수 하나에만 둔다** (`index.html` CONFIG 위). 공장 문서의 SSOT = 공유드라이브 `07_공장`.
  `DriveDocs.scan()` 이 제품별 성적서 4슬롯 + 기기 13대 검교정 폴더를 훑어 **원본 보유 여부**를 본다.
  ⚠️ 기기 폴더는 `기기/회차 폴더/파일` 로 **한 겹 더 깊다** — 얕게 보면 전부 '없음'이 된다.
  ⚠️ **파일명·폴더명에서 유효기한을 추측하지 말 것.** 만료 판정 SSOT 는 시트(`시험성적서`·`기기관리`)다.
  폴더 트리·ID 표 = `docs/FACTORY_MASTER_DATA.md` §1-1~1-3. 검증: `node tests/drive-docs.test.js`
- **성적서·검교정 원본은 앱에서 올린다** (`#haccp-std` 성적서 탭 · 실무자). 업로드 뒤 유효기한/차기 검교정일을
  시트에 넣는 데까지가 한 흐름이다 — **파일만 올리면 만료 알람이 안 울린다.** 삭제 기능은 두지 않는다(기록 보관 2년).
  ⚠️ 붙여넣기·드롭 필터(`_sfAcceptsFile`)는 input 의 `accept` 를 따른다. 이미지로 하드코딩하면 PDF 성적서가 조용히 버려진다.
- **`stepType='heat'`** = 가열공정 CCP-3B 기록(호기별 행). 판정은 `heatJudge()` 하나만 쓴다 —
  온도·시간 숫자를 화면·검증에 다시 쓰지 말 것. 이탈 시 개선조치 미기재면 공정 완료가 막힌다.
- 앙버터 호두과자 SOP 는 `#haccp-std` 「SOP 생성」(`hsSeedAngbutterSop`)으로 기준서 값에서 한 번에 만든다 — 손으로 옮겨 적지 말 것.
- 공장 기준정보 원천(품목보고번호·배합비·설비·성적서) = `docs/FACTORY_MASTER_DATA.md`

### UI 함정
- **`.btn-outline` 은 단독으로 쓰면 안 된다** — `.btn` 기본값인 흰 글씨·흰 테두리라 흰 카드 위에서 보이지 않는다.
  반드시 색상 modifier 와 짝짓는다: `btn-outline btn-pri` / `btn-gray` / `btn-info` / `btn-warn` / `btn-err`.
  (2026-08-05 라이브 검수에서 버튼 4개가 통째로 안 보이던 실장애)

## 데이터 모델

### 시트 컬럼 인덱스
- `작업지시서`: [0]=woId, [1]=sopId, [3]=fgItemCode, [4]=name, [5]=targetQty, [7]=packCount, [8]=email, [9]=시작, [10]=완료, [11]=냉동시작, [12]=상태, [13]=lastStep, [14]=fgLotId, [15]=승인자, [16]=승인시각, [17]=비고, [19]=pdfUrl
- `원재료LOT`: [0]=ID, [1]=itemCode, [4]=qty, [5]=unit, [6]=mfgDate, [7]=expDate, [8]=remain, [9]=status, [10]=indivCount, [11]=temp
- `완제품LOT`: [0]=ID, [1]=itemCode, [3]=qty, [5]=prodDate, [6]=expDate, [7]=woId, [8]=remain, [9]=status, [10]=packCount
- `원재료개별 / 완제품개별`: [0]=indivId, [1]=lotId, [2]=itemCode, [3]=status, [4]=qty/dt, [5]=note
- `완제품품목`: [8]=packsPerBox, [10]~[17]=규제정보(`FG_EXT`), **[18]=공급단가(원/봉·VAT 제외)** — 손익 매출의 유일한 단가 소스
- `원재료품목`: [13]=매입단가(원/기본단위) — ERP `SmartFactoryKpi.rawInventory()` 도 이 칸을 읽는다
- `공장손익`(자동 생성): [0]=period(YYYY-MM), [1]=항목(노무비/제조경비), [2]=금액, [3]=메모, [4]=등록자, [5]=등록일시

### 공장 손익 (`#pnl` · 관리자 전용)
**제조이익 = 매출 − (재료비 + 노무비 + 제조경비)**. 집계는 `FactoryPnl.build()` 하나뿐 — 화면은 그리기만 한다.
- 매출 = `출하기록` 수량 × `완제품품목[18]` 공급단가. **실청구액이 아니라 단가×물량 추정**이다.
- 재료비 = `공정기록`(stepType='input') `stepData.inputQty`(kg) × `원재료품목[13]`. 발생 기준.
  `구매주문[9] totalAmt` 합(=실매입·현금 기준)은 **참고로만** 보여주고 손익에 넣지 않는다(차이 = 재고 증감).
- 노무비·제조경비 = `공장손익` 시트 수기. 급여·임차료는 공장 시트에 없다 — ERP 회계(05) 결산의
  `인건비(공장)`·`공장경비`와 같은 값을 넣는 자리다.
- ⚠️ **물량은 전부 봉(1봉 5kg)으로 정규화**한다(`FactoryPnl.toPacks`). 시트마다 kg·박스가 섞여 있어
  그대로 더하면 배수로 틀린다(ERP 재고평가에서 실제로 3배 과대평가 사고). 단위가 `kg`면 ÷5, `box`면 ×packsPerBox.
- ⚠️ 공급단가는 품목당 **현재값 1개**뿐 → 단가를 바꾸면 **과거 달 매출도 같이 움직인다**(월별 단가 이력 없음).
- 감가상각·이자·판관비는 **넣지 않는다** — 여기 이익은 제조이익이지 순이익이 아니다.
- 검증: `node tests/factory-pnl.test.js`

### 상태값 (status 필드)
- WO `[12]`: 진행중 / 승인대기 / 냉동보관중 / 출하가능 / 출하완료 / 승인완료 / 거부-재작업 / 거부-폐기 / **삭제**
- 원재료 LOT `[9]`: 미사용 / 사용중 / 소진 / 폐기 / **입고취소**
- 완제품 LOT `[9]`: 냉동보관중 / 출하가능 / 출하완료 / 폐기 / 소진 / **삭제** / **채번예약** (atomic claim placeholder)

### LOT ID 규칙 (`generateLotId`)
- 형식: `{prefix}-{itemNum}-{mfgYY MM DD}-{expYY MM DD}-{NNN}`
- 예: `FG-002-260427-261027-001` (FG002, 2026-04-27 제조, 2026-10-27 만료, 1번)
- **Atomic Claim**: QR라벨 단계에서 채번 즉시 placeholder row 적재 → 동시 채번 충돌 방지

## 환경

- **Repo**: `doganzi/maytutu-smartfactory` (private)
- **배포**: GitHub Pages 자동 (main 브랜치) → https://doganzi.github.io/maytutu-smartfactory/
- **백엔드**: Google Sheets + Drive (사용자 OAuth, 클라이언트 사이드)
- **단일 파일**: `index.html` (~430KB) + `manifest.json` (PWA)
- **소스 외 접근**: Sheets ID `1_koe58lReouU_ZZxzUh5SU8gUkuHjlwb1LNjZbJGSLU`
  - Claude Code 환경에서 직접 접근 불가 (OAuth 자격증명 없음)
  - 데이터 확인 필요 시 사용자가 시트 일부를 채팅에 붙여넣어 줘야 함

## 개발 / PR 규칙

- 개발 브랜치: `claude/continue-smart-factory-eiuWj`
- 머지: PR → squash merge (선형 히스토리)
- 모든 변경 전 진행중 작업 영향 점검 — wo-execute / 진행중 WO 데이터 보존 우선
- LiveSync 자동 재렌더는 입력 화면 (`wo-create`, `bom-calc`, `req-form`, `data-audit`)에서 스킵 — 입력값 보존
